version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 14
    commands:
      - npm install -g @angular/cli

  pre_build:
    commands:
      - npm install

  build:
    commands:
      - ng build --prod

artifacts:
  files:
    - dist/* # This assumes your Angular output is in the "dist" directory
